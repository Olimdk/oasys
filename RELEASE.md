# Releasing OASYS

OASYS is installed with a one-line `curl | bash` installer that lives at the
repo root (`install.sh`). Every shipped version gets its **own curl command**,
so users can install or roll back to an exact release without guessing a ref.

## Install commands

Latest (rolling `main`):

